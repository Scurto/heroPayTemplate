spring:
  application:
    name: merchant-service

server:
  port: 8891

eureka:
  client:
    registerWithEureka: true
    fetchRegistry: true
#    serviceUrl:
#      defaultZone: http://localhost:8761/eureka/ - configured by Spring Cloud Config
  instance:
    hostname: localhost